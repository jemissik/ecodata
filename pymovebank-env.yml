name: pymovebank-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - xarray
  - cfgrib
  - geopandas
  - shapely<1.8.2
  - matplotlib
  - pandas
  - cartopy
  - ipython
  - ipykernel
  - pip
  - pip:
    - wget
    - requests
    - datasize
    - -e .  # Installs pymovebank in editable mode