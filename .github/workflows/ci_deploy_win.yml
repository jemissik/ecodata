name: ci_deploy_win
on:
  push:
  pull_request:
    branches:
      - develop
      - main
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Mambaforge
        uses: conda-incubator/setup-miniconda@v2
        with:
            miniforge-variant: Mambaforge
            miniforge-version: latest
            activate-environment: pmv
            environment-file: environment-clean-build.yml
            use-mamba: true

      - name: Install package
        shell: pwsh
        run: |
          cd ~
          pwd
          ls
          pip install git+https://github.com/jemissik/pymovebank.git@${{ github.sha }}
          python -c 'import pymovebank; print(pymovebank.available()); import pymovebank.app.apps.tracks_explorer_app'
